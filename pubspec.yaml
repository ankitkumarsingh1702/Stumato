name: hushh_for_students # Use underscores instead of spaces
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  webview_flutter: ^4.9.0
  url_launcher: ^6.3.0
  firebase_core: ^3.4.0
  cloud_firestore: ^5.4.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/oac.jpg
    - lib/assets/thapa.jpg
